---
# Cloudflare DNS Updater Configuration

cloudflare:
  zone_id: "YOUR_ZONE_ID"
  api_token: "YOUR_API_TOKEN"

options:
  proxied: true   # Default for all domains: true (Orange Cloud), false (Grey Cloud, DNS only).
  ttl: 1          # Default TTL: 1 (Auto), or 60-86400 seconds.
  interface: ""   # Optional: network interface for local IPv6 detection (e.g. "eth0").
  create_if_missing: false   # true: create records listed here that do not exist yet.

domains:
  # Example 1: Update both IPv4 (A) and IPv6 (AAAA) if detected (Default behavior)
  - name: "example.com"

  # Example 2: Update only IPv4 (A record)
  - name: "ipv4.example.com"
    ip_type: "ipv4"

  # Example 3: Update only IPv6 (AAAA record)
  - name: "ipv6.example.com"
    ip_type: "ipv6"

  # Example 4: Disable Proxy (DNS Only)
  - name: "direct.example.com"
    proxied: false

  # Example 5: Explicitly update both
  - name: "both.example.com"
    ip_type: "both"

  # Example 6: Create this record if it does not exist yet in Cloudflare
  - name: "new.example.com"
    create_if_missing: true

notifications:
  telegram:
    enabled: false
    bot_token: ""
    chat_id: ""

  discord:
    enabled: false
    webhook_url: ""
