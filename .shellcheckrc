# ShellCheck configuration file
# SC2154: variables are assigned dynamically in config.sh via parse_config
# SC2034: variables might appear unused in source modules but are used in main.sh
disable=SC2154,SC2034
# Enable some optional checks
enable=add-default-case,avoid-nullary-conditions,quote-safe-variables
# Exclude tools/ directory from being recursively checked as scripts if it contains C source
# (ShellCheck doesn't check .c files, but some wrappers might)